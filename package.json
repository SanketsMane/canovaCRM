{
  "name": "canova-crm",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && npm start"
  },
  "dependencies": {
    "react-icons": "^5.5.0"
  }
}
